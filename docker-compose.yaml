version: '3'

services:
#  nginx:
#    container_name: nginx
#    image: nginx:alpine
#    ports:
#      - 80:80
#    volumes:
#      - ./nginx/conf.d:/etc/nginx/conf.d
#    depends_on:
#      - api

  api:
    container_name: api
    image: api:maven
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - 8080:8080
    volumes:
      - /var/app
    restart: always